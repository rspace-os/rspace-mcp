[project]
name = "rspace-mcp"
version = "0.2.0"
description = "RSpace MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fastmcp>=3",
    "rspace-client>=2.7.2",
]
